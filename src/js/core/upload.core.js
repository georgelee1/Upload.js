/**
 * The core is the engine that handles the uploading and deleting of files.
 */
export function core(http, opts) {
  /**
   * Upload one or more files.
   */
  function upload(...files) {

  }

  return {
    upload,
  };
}
